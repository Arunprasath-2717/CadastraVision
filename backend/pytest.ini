[pytest]
asyncio_mode = auto
# Run async tests under asyncio only — trio is not installed
anyio_backends = asyncio
testpaths = tests
addopts = -v --tb=short
markers =
    anyio: mark test as running with anyio
